import { Flex, Center, Text, Container, useBreakpointValue, Button } from "@chakra-ui/react";
import { Box, HStack } from "@chakra-ui/layout";
// import { Image } from "@chakra-ui/react";
import Image from "next/image";
import type { NextPage } from "next";
import Head from "next/head";
import illustrations from "../public/assets/images";

const Illustration: NextPage = () => {
  return (
    <>
      <Head>
        <title>Nadia Ramirez | ILLUSTRATION</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Box>
        <HStack spacing="24px" flexWrap="wrap">
          {illustrations.map((img) => {
            return (
              <Box key={img.id} flex={1} width="200px">
                <Image src={img.src} alt={img.alt} />
              </Box>
              
            );
          })}
        </HStack>
      </Box>
    </>
  );
};

export default Illustration;
