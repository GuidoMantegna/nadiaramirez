import { Center } from "@chakra-ui/react";
import type { NextPage } from "next";
import { useState } from "react";

const UI: NextPage = () => {
  return (
    <Center>
      UI/UX
    </Center>
  );
};

export default UI;
